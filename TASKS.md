# Рабочий журнал

Статус задач. Что именно изменилось в коде — в [CHANGELOG.md](CHANGELOG.md), здесь не дублируется.

## IN_PROGRESS

—

## Запланировано

### 0.3.0 — UX из PR #1

Разобрать [PR #1](https://github.com/stufently/free-zakupki-gov-ru-monitor/pull/1) от @doovoos по частям, каждую с тестами:

- группировка записей по лентам в popup со сворачиванием;
- кнопки «Сбросить ленту» и «Сбросить все ленты»;
- показ последних 10 записей при первом запуске ленты, без уведомлений;
- дедупликация `recent`. Ключом брать `feedKey + item.id`, а не один `link`: по `link` схлопнутся находки одной закупки из разных лент.

### 0.3.x — надёжность

- Повторы с backoff при сетевых сбоях.
- `ETag` / `If-Modified-Since`, чтобы не тянуть неизменившийся фид.
- Реальная фикстура живого RSS ЕИС в `tests/fixtures/` (с датой и исходным URL). **Нужен доступ к zakupki.gov.ru** — с сервера разработки портал недоступен (прокси отдаёт 502), сохранить фид может только пользователь из РФ.
- Проверка на самой старой поддерживаемой версии Chrome (119). Смоук-тест гоняется на текущем Chromium, где доступны и `getContexts`, и `hasDocument`; несовместимость с 119–149 поймал бы только запуск на старом билде.

### Бэклог

- Сборка под Firefox (там MV3-фон имеет DOM, offscreen не нужен).
- Импорт/экспорт лент в OPML.
- Опциональные сторонние RSS через `optional_host_permissions`.

## COMPLETED

- **2026-07-29 — 0.2.0.** Починен фатальный `DOMParser` в service worker (разбор перенесён в offscreen), добавлена диагностика лент и подсказка про сертификат Минцифры, обновлены CI и README. Ветка `fix/mv3-offscreen-parser`.
